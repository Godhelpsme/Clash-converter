# Copy this file to ".env" and adjust values as needed.
# It is used by docker-compose for variable substitution.

# Exposed port on the host (mapped to Nginx 80 inside the container).
PORT=3000

# Trust proxy headers (required when behind Nginx/reverse proxy).
TRUST_PROXY=true

# Authentication (REQUIRED in production).
# AUTH_ENABLED defaults to true in docker-compose.
# AUTH_USERNAME and AUTH_PASSWORD are REQUIRED - docker-compose will fail without them.
AUTH_ENABLED=true
AUTH_USERNAME=your-username
AUTH_PASSWORD=your-secure-password

# JWT secret (REQUIRED in production when AUTH_ENABLED=true).
# In development/test, the backend will fall back to an insecure default if not set.
AUTH_JWT_SECRET=change-me-to-a-long-random-secret

# Token expiry time in milliseconds (default: 24 hours).
# AUTH_JWT_TTL_MS is optional; if empty, backend falls back to AUTH_TOKEN_TTL_MS.
AUTH_JWT_TTL_MS=
AUTH_TOKEN_TTL_MS=86400000

# Comma-separated allowed CORS origins.
# In production, you MUST set this to your actual domain(s) if cross-origin access is needed.
# Empty means CORS is disabled (recommended for same-origin deployments).
ALLOWED_ORIGINS=

# Upload file size limit in MB (default: 5, minimum: 1).
UPLOAD_MAX_FILE_SIZE_MB=5

# YAML Worker pool configuration.
YAML_WORKER_POOL_SIZE=

# YAML task timeout in milliseconds (default: 15000).
YAML_TASK_TIMEOUT_MS=15000

# Maximum pending YAML tasks (default: 200).
YAML_MAX_PENDING=200

# Maximum YAML alias count to prevent YAML bomb attacks (default: 100).
YAML_MAX_ALIAS_COUNT=100
